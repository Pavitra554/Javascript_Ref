const print =(name="default")=>{
    console.log(name);
}
print();
print("Pavitra");

//if we want to destructure an object but if object doesn't exists the we 
//destructure an empty object like function fun(data,{lable}={}){}
